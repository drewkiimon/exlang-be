```
npm install
npm run dev
```

```
open http://localhost:3000
```

## Postgresql

- OrbStack is nice
- Run a simple `docker run --name my-little-db -e POSTGRES_USER=admin -e POSTGRES_PASSWORD=password -e POSTGRES_DB=mydb -d postgres`
